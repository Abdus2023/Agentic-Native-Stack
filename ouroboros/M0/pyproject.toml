[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ourob-m0"
version = "0.1.0"
description = "Deterministic self-hosting autonomous engineering kernel"
requires-python = ">=3.11"
dependencies = ["cryptography>=44,<47"]

[tool.pytest.ini_options]
testpaths = ["tests"]
